# 学习库契约

`短视频拆解库` 是外部短视频的事实拆解和可参考结构；`句式模板库` 是经人工审核后可复用的语言表达。它们不是脚本，也不替代 `structured_script`。

拆解记录必须能回溯至来源视频、帧/ASR 证据和时间码。`可参考的叙事模板`只做检索摘要，固定为四句：`适合：`、`结构：`、`主要证明：`、`不适合：`。`逐镜头复刻模板`才保存逐秒的原镜头→新镜头改写模板。

句式由 AI 从高置信 ASR/OCR 或已验证脚本中提炼为`待审核`候选；人工只审核为`可用`、`已驳回`或`已合并`，不负责逐条人工发掘。只有`可用`句式可被脚本读取。

每条句式只能服务一个语言任务（钩子、问题提出、产品引入、原理解释、操作说明、结果证明、转折或 CTA），模板以`[主体]`、`[产品]`等槽位表达。原句只是学习证据，不能被当作可直接照搬的成稿。
