# Legacy archive

These files are retained for historical comparison and migration only. They are not workflow inputs and must not be loaded during a normal run. Current workflows live under `references/workflows/`, shared domain contracts under `references/domain/`, and invariants under `references/invariants/`.
